language: python
python:
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
#coverage run -m unittest tests/online_test.py
script:
 - python -m unittest tests/online_test.py -v
notifications:
  email: 
    - koreal6803@gmail.com
